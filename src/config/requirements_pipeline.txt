# Dependencias para la GUI unificada del Pipeline CMF (run_pipeline_gui.py).
# El venv se crea desde un Python que tenga tkinter (p. ej. pyenv 3.12.2);
# tkinter NO se instala por pip, viene del Python base.
#
# Nota: la consolidación (CMF_EXTRACT) corre en SU propio intérprete; sus
# dependencias (Arelle, etc.) NO van en este venv.

selenium==4.16.0
pandas==2.1.4
beautifulsoup4==4.12.2
XlsxWriter==3.1.9
openpyxl==3.1.2

# Descarga directa HTTP / reintentos y subida (uploader FinDataChile, diferido)
requests>=2.31
urllib3>=1.26

# Gestión automática de ChromeDriver para selenium
webdriver-manager>=4.0
